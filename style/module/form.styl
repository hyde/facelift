/*
   - error colors should be subtle, red can be naive
   - concider an error icon
   - input nested inside label gets focused with a click
   - no support for input type="button", input type="image"
   - fieldset can add structure

   - select = custom skin
   - textareas should autogrow
*/

inputs()

   input, textarea
      font-size 100%
      border-radius uh
      padding u
      border 1px solid gray4

      if shadow-input
         box-shadow inset 0 0 2px rgba(black, .2)

      &:focus
         border-color darken(gray4, 25)
         outline 0

         if shadow-input
            box-shadow inset 0 0 1px rgba(black, .1), 0 0 4px focus_color

      &[disabled], &[readonly]
         background #f5
         cursor not-allowed
         border-color gray4
         box-shadow none


   // autogrow is better
   textarea
      resize none
      overflow hidden
      transition height fast

   label
      margin 0 0 u
      display inline-block
      cursor pointer

      span
         display block
         font-size 90%
         color #c

   // fieldset groups inputs side by side
   fieldset
      border 0

      label
         display inline-block
         padding-right u
         margin u 0

   legend
      font-size large


   // hide number spinners
   [type=number]
      &::-webkit-outer-spin-button
      &::-webkit-inner-spin-button
         margin 0
         -webkit-appearance none

if classify
   /*! Form */
   inputs()