/*
   - error colors should be subtle, red can be naive
   - concider an error icon
   - input nested inside label gets focused with a click
   - no support for input type="button", input type="image"
   - fieldset can add structure

   - select = custom skin
   - textareas should autogrow
*/

inputs()

   input, textarea
      font-size 100%
      border-radius uh
      padding u
      transition medium
      border 1px solid line_color

      if shadow-input
         box-shadow inset 0 0 2px rgba(#0, .2)

      &:focus
         border-color darken(line_color, 25)
         outline 0

         if shadow-input
            box-shadow inset 0 0 1px rgba(#0, .1), 0 0 4px focus_color

      &[disabled], &[readonly]
         background #f5
         cursor not-allowed
         border-color line_color
         box-shadow none


   // autogrow is better
   textarea
      resize none
      overflow hidden

   label
      margin 0 0 u
      display block
      cursor pointer

   // fieldset groups inputs side by side
   fieldset
      border 0

      label
         display inline-block
         padding-right u
         margin u 0

   legend
      font-size large


if classify
   inputs()