
/*
   - button for action, link for link
   - a site wide element - many times used outside form element
   - ideally only one primary action per page = visually prominent
   - dropdown buttons are overkill, a wrong UI pattern
*/

hover-bg(color)
   background color
   transition background-color fast

   &:hover
      background lighten(color, 5)

   &:active
      background darken(color, 5)

hover-gradient(color)
   linear-gradient color

   &:hover
      linear-gradient lighten(color, 5)

   &:active
      background-image none
      background color


button(color)
   padding uh u2
   border-radius u4
   font 110%/1.4 #e center
   cursor pointer
   border 0
   text-shadow 0 -1px 0 rgba(#0, 0.25)
   hover-bg color

   if button-gradient
      hover-gradient color

   &:active
      box-shadow inset 0 2px 4px rgba(#0, 0.15), 0 1px 2px rgba(#0, 0.05)

   &:focus
      outline thin dotted #3
      outline 2px auto -webkit-focus-ring-color
      outline-offset 2px

   &:disabled
      cursor default
      background-image none
      background #c
      box-shadow none

if classify
   button
      button button_color
