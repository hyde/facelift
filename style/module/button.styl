
/*
   - button for action, link for link
   - a site wide element - many times used outside form element
   - ideally only one primary action per page = visually prominent
   - dropdown buttons are overkill, a wrong UI pattern
*/
button(color = button_color)
   padding .4em 1.5em
   border-radius u5
   font 105%/1 #f center
   background color
   cursor pointer
   border 0

   &:hover
      linear-gradient rgba(#f, .2) rgba(#0, .1) 100%

   &:active
      box-shadow inset 0 2px 4px rgba(#0, 0.7), 0 1px 2px rgba(#f, 0.5)
      background-image none

   &:focus
      outline thin dotted gray1
      outline 2px auto -webkit-focus-ring-color
      outline-offset 2px

   &:disabled
      cursor default
      background-image none
      background gray4
      box-shadow none


if classify
   /*! Buttons */
   button, .button
      button button_color
